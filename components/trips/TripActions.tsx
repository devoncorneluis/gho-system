type Props = {  status: string | null;  onStart: () => void;  onComplete: () => void;  onCancel: () => void;};export default function TripActions({  status,  onStart,  onComplete,  onCancel,}: Props) {  return (    <div className="mt-5 rounded-xl border bg-white p-4">      <h3 className="mb-4 text-lg font-bold text-[#061B33]">        🚦 Trip Actions      </h3>      <div className="flex flex-wrap gap-3">        {status !== "In Progress" &&          status !== "Completed" &&          status !== "Cancelled" && (            <button              onClick={onStart}              className="rounded-lg bg-green-600 px-5 py-2 font-bold text-white"            >              ▶ Start Trip            </button>          )}        {status === "In Progress" && (          <button            onClick={onComplete}            className="rounded-lg bg-blue-600 px-5 py-2 font-bold text-white"          >            ✔ Complete Trip          </button>        )}        {status !== "Completed" &&          status !== "Cancelled" && (            <button              onClick={onCancel}              className="rounded-lg bg-red-600 px-5 py-2 font-bold text-white"            >              ✖ Cancel Trip            </button>          )}      </div>    </div>  );}
