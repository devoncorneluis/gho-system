type Passenger = {  id: string;  trip_id: string;  full_name: string | null;  phone: string | null;  pickup_address: string | null;  pickup_area: string | null;  pickup_status: string | null;};type Props = {  tripId: string;  passengers: Passenger[];};export default function PassengerManifest({  tripId,  passengers,}: Props) {  const tripPassengers = passengers.filter(    (passenger) => passenger.trip_id === tripId  );  return (    <div className="mt-4 rounded-xl border bg-white p-4">      <h3 className="mb-4 text-lg font-bold text-[#061B33]">        📄 Passenger Manifest      </h3>      {tripPassengers.length === 0 ? (        <p className="text-gray-500">          No passengers assigned.        </p>      ) : (        <div className="space-y-3">          {tripPassengers.map((passenger, index) => (            <div              key={passenger.id}              className="rounded-lg border bg-gray-50 p-3"            >              <p className="font-bold">                {index + 1}. {passenger.full_name}              </p>              <p className="text-sm text-gray-600">                📞 {passenger.phone || "No phone"}              </p>              <p className="text-sm text-gray-600">                📍 {passenger.pickup_address || passenger.pickup_area}              </p>              <p className="font-semibold">                Status: {passenger.pickup_status || "Waiting"}              </p>            </div>          ))}        </div>      )}    </div>  );}
