type Driver = {  id: string;  full_name: string | null;};type Vehicle = {  id: string;  vehicle_name: string | null;  registration_number: string | null;};type TripAssignment = {  id: string;  driver_name: string | null;  vehicle_name: string | null;  vehicle_registration: string | null;};type Props = {  trip: TripAssignment;  drivers: Driver[];  vehicles: Vehicle[];  onSave: () => void;};export default function AssignmentPanel({  trip,  drivers,  vehicles,  onSave,}: Props) {  return (    <div className="mt-4 rounded-xl border bg-white p-4">      <h3 className="mb-4 text-lg font-bold text-[#061B33]">        🚐 Assignment      </h3>      <div className="grid grid-cols-1 gap-4 md:grid-cols-3">        <div>          <p className="text-xs font-bold text-gray-500">DRIVER</p>          <select            value={trip.driver_name || ""}            onChange={() => {}}            className="mt-2 w-full rounded-lg border p-2"          >            <option value="">Select Driver</option>            {drivers.map((driver) => (              <option key={driver.id} value={driver.full_name || ""}>                {driver.full_name}              </option>            ))}          </select>        </div>        <div>          <p className="text-xs font-bold text-gray-500">VEHICLE</p>          <select            value={trip.vehicle_name || ""}            onChange={() => {}}            className="mt-2 w-full rounded-lg border p-2"          >            <option value="">Select Vehicle</option>            {vehicles.map((vehicle) => (              <option                key={vehicle.id}                value={vehicle.vehicle_name || ""}              >                {vehicle.vehicle_name} ({vehicle.registration_number})              </option>            ))}          </select>        </div>        <div>          <p className="text-xs font-bold text-gray-500">            REGISTRATION          </p>          <p className="mt-3 font-semibold">            {trip.vehicle_registration || "Not Assigned"}          </p>        </div>      </div>      <button        onClick={onSave}        className="mt-5 rounded-lg bg-[#061B33] px-5 py-2 font-bold text-white"      >        💾 Save Assignment      </button>    </div>  );}
